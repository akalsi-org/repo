#!/usr/bin/env bash
# repo-managed-hook
set -euo pipefail

root="$(git rev-parse --show-toplevel)"
"$root/repo.sh" agent_check --stale-only
"$root/repo.sh" lint
"$root/repo.sh" mypy --strict tools/agent_check.py tools/ideas.py
